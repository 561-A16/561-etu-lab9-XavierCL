sudo: required

language: csharp
cache:
  directories:
  - $HOME/packages

services:
  - docker

script:
  - docker build -t webapp:test .
  - docker run -it -v $HOME/packages:/root/.nuget/packages webapp:test